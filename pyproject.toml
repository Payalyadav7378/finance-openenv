[project]
name = "finance-openenv"
version = "0.1.0"
description = "AI Finance Coach OpenEnv"
dependencies = [
    "numpy",
    "pydantic",
    "flask",
    "openenv"
]

[project.scripts]
server = "server.app:app"
